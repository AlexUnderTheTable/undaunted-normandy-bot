# TODO — Undaunted Normandy веб-бот

## Сейчас

- [ ] Проверить правки вживую за столом (автотесты и сборка уже
      зелёные, см. «Сделано»)
- [ ] Обкатать правило эндшпиля (`OPEN_QUESTIONS.md` п. 12) — новая
      механика сверх исходного скрипта, ещё не проверена живой партией
- [ ] Обкатать выбор юнита при пополнении (`OPEN_QUESTIONS.md` п. 13)
      — тоже новое правило, официальные правила отдают выбор на
      усмотрение игрока, единого предписания нет

## Дальше

- [ ] Попробовать самому в реальной партии, собрать замечания по
      формулировкам/UX
- [ ] git push при новых изменениях (после первого push)

## Сделано (для истории, не трогать без причины)

- [x] Пополнение теперь говорит, каким юнитом пополняться (2026-09-05):
      памятка `reinforceUnitChoice` — приоритет как при выборе цели
      атаки, с учётом привязки к отделению у Squad Leader
      (`OPEN_QUESTIONS.md` п. 13). Заодно починены мёртвые поля
      `generalRule`/`reinforceRule` — существовали в данных с самого
      начала, но нигде не показывались игроку; перенесены в `memos` и
      подключены
- [x] Правило эндшпиля (2026-09-05): новый вопрос «Игрок сейчас
      впереди по очкам сценария?» сразу после гейта «жетон на поле» у
      всех боевых юнитов — если да, бот играет агрессивнее (предпочитает
      атаку). Этого нет в `bot_script.txt`, добавлено по запросу
      (`OPEN_QUESTIONS.md` п. 12). Реализовано в `index.ts` без правки
      JSON юнитов — обе ветки нового вопроса ведут в один и тот же
      исходный скрипт по ссылке на объект
- [x] Правило выбора маршрута (2026-09-05): кратчайший путь, обход
      юнитов игрока только если он бесплатный (не длиннее), иначе бот
      идёт напролом — линия огня как критерий не подошла, поля в
      Undaunted маленькие (`OPEN_QUESTIONS.md` п. 11). Памятка
      `pathSelectionRule` привязана к 10 узлам-действиям с
      перемещением; `memo` теперь может быть массивом, чтобы не терять
      уже стоявшие там памятки (ценность точки, выбор цели)
- [x] Названия приведены к русской коробке (2026-09-05): Сержант →
      «Командир взвода», Командир → «Командир отделения», действие
      разведчика RECON → «Наблюдать» (`OPEN_QUESTIONS.md` п. 10)
- [x] Тесты (2026-09-05): vitest, 172 теста в 5 файлах —
      `npm test`. Покрыто: формулы калькуляторов (§4-6, §11),
      структура всех деревьев + резолв памяток, UI-флоу (выбор цели →
      юнит → вопросы → действие → назад/заново, темы, localStorage) и
      регрессии на замечания обкатки (`OPEN_QUESTIONS.md` пп. 6-9).
      Тесты проверены мутациями: возврат каждой из четырёх ошибок
      роняет ровно те тесты, что её сторожат
- [x] Логика калькуляторов вынесена из компонентов в `src/logic/`
      (`keyPoint.ts`, `targetSelection.ts`, `mortar.ts`) — поведение то
      же, но стало тестируемым
- [x] Node поставлен портативно (2026-09-05): распакованный zip в
      `~/.local/node`, в PATH не прописан — см. `HOSTING.md`, раздел
      «Портативный Node». `npm install` + `npm run build` проходят,
      `tsc -b` без ошибок
- [x] Правки по итогам первой обкатки (2026-09-05): гейт на сыгранные
      карты и отряд при воодушевлении, гейт «жетон юнита на поле»,
      починен застревавший разведчик, всюду уточнено «итоговая защита»
      (детали и обоснования — `OPEN_QUESTIONS.md` пп. 6-9)
- [x] Весь `bot_script.txt` переведён в JSON-дерево решений
      (`src/data/`), см. `PLAN.md` для структуры
- [x] Vite + React + TypeScript, весь флоу (выбор цели ИИ → юнит →
      вопросы → действие) работает, проверено в браузере
- [x] Калькуляторы: ключевая точка, выбор цели атаки, квадрат миномёта
- [x] Адаптивная вёрстка под мобильные экраны
- [x] Военно-полевая тема + переключатель светлая/тёмная
- [x] git init + 2 коммита, `HOSTING.md` с инструкцией по деплою

## Открыто

- Нужен ли учёт инициативы/порядка карт в v1, или это полностью ручное
  (physical) действие игрока? (пока решили: не автоматизируем в v1)
- Кнопка "назад" в дереве реализована — проверить, устраивает ли на
  практике после игры вживую
